{
  "rules": {
    "providerLocations": {
      "$bookingId": {
        ".read": "auth != null",
        ".write": "auth != null",
        ".validate": "newData.hasChildren(['lat', 'lng', 'updatedAt'])"
      }
    }
  }
}
