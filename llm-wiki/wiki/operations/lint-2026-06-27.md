---
tags: [lint, health-check]
date: 2026-06-27
related:
  - [[Functions Lint Report]]
  - [[Chat Media Implementation]]
  - [[Online Projects]]
  - [[Version Cache Busting]]
  - [[PH Location Data]]
  - [[Send Contact Email]]
---

# Lint Report: Wiki Health Check (2026-06-27)

Lint pass based on recent git commits (commits `b729ffe3` through `85aec7b8`, 25 wiki pages).

## 🔴 Contradictions

### 1. Chat Media Implementation page is completely stale

`decisions/chat-media-implementation.md` shows a completion table with phases 1–6 marked **❌ Not implemented**, but [[Chat System]] and [[Media and Images]] confirm every one of those is **fully implemented**:

| Phase | Claimed Status | Actual Status |
|-------|---------------|---------------|
| 1b: `uploadChatAttachment` action | ❌ | ✅ Implemented as `initChatAttachment` (two-step) |
| 1c: `deleteChatAttachment` action | ❌ | ✅ Implemented `deleteChatAttachmentHandler` |
| 2: `onMessageCreated` attachment handling | ❌ | ✅ Handles attachments, generates file-type-aware notifications |
| 3: `sendMediaMessage()` in chatCanisterService | ❌ | ✅ Line 375, supports 1–5 attachments with caption |
| 4: `uploadChatAttachments()` in mediaService | ❌ | ✅ Two-step flow, direct Storage upload |
| 5: `sendMediaMessage()` in useChat.tsx | ❌ | ✅ Groups by type, sends separate messages per group |
| 6: ChatAttachmentPicker + Preview UI | ❌ | ✅ `ChatAttachmentStrip`, `ChatAttachmentPreview`, paste support |

Only phases 7 (Mobile) and 8 (Storage rules) remain incomplete. The page needs a full rewrite.

**Severity**: High — affects [[Chat Media Implementation]].

### 2. Functions Lint Report claim #9 is outdated

`operations/functions-lint-report.md` (line 70): "chat.js still has no attachment handling" — this was true at initial lint time but [[Chat System]] (updated Jun 19–20) now documents that `onMessageCreated` in chat.js has attachment-aware notification previews, guard passes for attachment-only messages, and OneSignal push with file-type text.

**Severity**: Medium — affects [[Functions Lint Report]].

## 🟡 Orphans (No Inbound Wikilinks)

Pages listed in `index.md` but **not referenced** by any other wiki page via `[[Page Name]]`:

| Page | Notes |
|------|-------|
| [[Online Projects]] | Created Jun 19, no other page links to it. Should be cross-referenced from [[Service Creation Workflow]] (mode extension) and [[Booking System]] (parallel lifecycle). |
| [[Version Cache Busting]] | Created Jun 16, never linked. Should be referenced from [[Firebase Hybrid Architecture]] (deployment) and [[Frontend Overview]] (build pipeline). |
| [[PH Location Data]] | Created Jun 16 lint fix, never linked. Should be referenced from [[Service Creation Workflow]] (location step uses PH data) and [[Service Discovery and Listing]] (location filtering). |
| [[Send Contact Email]] | Standalone v1 function page, never linked as wikilink. [[Firebase Hybrid Architecture]] lists it in the function table but without wikilink. |

**Severity**: Low — easy wins, each needs one wikilink insertion in a relevant parent page.

## 🟢 Stale Claims

1. **`decisions/chat-media-implementation.md`** (entire page) — predates implementation; completion table and principles are now history rather than plan
2. **`operations/functions-lint-report.md`** claim #9 — chat.js gap closed
3. **`operations/fcm-push-notifications.md`** — describes FCM as primary delivery but frontend uses **OneSignal v16** as the push abstraction; FCM is the underlying layer

## 📋 Missing Cross-Refs (Concepts Without Pages)

| Concept | Mentioned In | Why It Needs a Page |
|---------|-------------|---------------------|
| **OneSignal** | firebase-hybrid-architecture, frontend-overview, services-layer, chat-system, fcm-push-notifications | Critical infrastructure — 5 pages reference it but no dedicated page explains the integration pattern |
| **TanStack Query** | frontend-overview, services-layer, media-and-images, state-and-hooks | Key architectural pattern — 5min stale, 24h GC, 2 retries applies across the frontend |
| **Firestore Security Rules** | online-projects (rules diff), chat-system (gap #5), media-and-images (gap #4) | Rules pattern documented piecemeal; no single reference page |
| **GCash / Payment Flow** | booking-system (payment lifecycle), service-and-booking-models (paymentMethod enum) | Complex payment flow with polling, escrow, admin release — deserves its own page |
| **Provider Profile (FrontendProfile)** | service-discovery (fetchProviderProfiles), state-and-hooks (useUserProfile), service-and-booking-models (EnhancedBooking) | Core entity type referenced across the stack, no definition page |

## 📊 Quick Stats

| Metric | Value |
|--------|-------|
| Total pages | 25 |
| Orphans (no inbound wikilinks) | 4 |
| New contradictions | 2 (1 high severity) |
| Stale claims | 2 (1 entirely stale page) |
| Uncovered concepts | 5 |

## Recommendations

1. **Rewrite** `decisions/chat-media-implementation.md` — demote past implementation progress to a "History" section, refocus on the remaining gaps (mobile, storage rules, thumbnails, progress)
2. **Add wikilinks** to the 4 orphan pages from their natural parent pages
3. **Update** `operations/functions-lint-report.md` claim #9 to reflect current chat.js attachment handling
4. **Consider new pages** for OneSignal, TanStack Query, and Firestore Security Rules
